import bindings/arduino ## Bindings for the core Arduino API
export arduino

import bindings/wstring ## Bindings for the String class
export wstring

import bindings/serial ## Bindings for the Serial class
export serial
